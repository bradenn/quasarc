<!DOCTYPE html>
<html lang="en">
<%- include("partials/header"); %>

<body>

  <%- include("partials/nav"); %>
  <br>
  <div class="container">
    <% for (var i = 0; i < post.length; i++) { %>
    <div class="row" style="margin-bottom: 4px;">

      <div class="col-md-8">
        <a href="/p/<%= post[i]._id %>" class="no-hover">
          <div class="card bg-light">
            <span class="card-title-span"><%= post[i].title %></span>
            <span class="card-sub-span">by <%= post[i].user %> • <%= post[i].section %> </span>
            <div class="card-body card-text-body-fade">
              <p class="card-text card-text-fade"><%= post[i].post %></p>

            </div>
          </div>
        </a>

      </div>

      <div class="col-md-4"></div>
    </div>
    <% } %>

  </div>

</body>

</html>
