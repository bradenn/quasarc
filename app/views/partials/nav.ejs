<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="/"><span class="text-brand">Q</span>uasarc</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <i class="fas fa-bars"></i>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">


    </ul>
    <ul class="navbar-nav mr-right" id="nav">
      <a href="/messages" class="btn btn-secondary nav-drop username"><i class="fas fa-envelope"></i></a>
      <a href="/new" class="btn btn-secondary nav-drop username"><i class="fas fa-plus-square"></i></a>
      <% if(user != null){ %>
      <div class="dropdown ">
        <button class="btn btn-secondary nav-drop dropdown-toggle username" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= user.username %>&nbsp;&nbsp;<% if(user.badge[0] === "lgbt") { %>badge<%}%><span class="float-right"><i class="fas fa-caret-down"></i></span>
        </button>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
          <a class="dropdown-item" href="/profile">profile</a>
          <a class="dropdown-item" href="/settings">settings</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="/logout">logout</a>
        </div>
      </div>

      <% } else{ %>
          <li class="nav-item">
            <a class="nav-link" href="/login">Login / Signup</a>
          </li>
          <% } %>

    </ul>
  </div>
</nav>
<% if(typeof post != 'undefined'){ %>

<% } %>
